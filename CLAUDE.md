# AgentSpec Runtime Note

AgentSpec uses `AGENTS.md` as the master repository instruction file.

Canonical behavioral rules are stored under `.agents/`.

Do not create conflicting duplicate instructions here.

Claude-compatible runtimes should follow:

1. `AGENTS.md`
2. `.agents/core/`
3. `.agents/context/`
4. relevant skills
5. relevant workflows
6. relevant checklists
